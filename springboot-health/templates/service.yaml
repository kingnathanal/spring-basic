apiVersion: v1
kind: Service
metadata:
  name: {{ include "springboot-health.fullname" . }}
  labels:
    {{- include "springboot-health.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springboot-health.selectorLabels" . | nindent 4 }}
